<li>
	{{burger_name}}

	<button class="change-devoured" 
		data-id="{{id}}" data-not-devoured="{{devoured}}">
		
		{{#if devoured}}Devour{{/if}}

	</button>

	<button  class="delete-burger"  data-id="{{this.id}}">Delete!</button>
</li>
